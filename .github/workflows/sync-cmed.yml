name: 'Sincronizar Base CMED'

on:
  schedule:
    # Todo dia 28 às 06:00 UTC (mesmo que o backend)
    - cron: '0 6 28 * *'
  workflow_dispatch: # Permitir execução manual

jobs:
  sync-cmed:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: |
        npm install axios xlsx fs-extra
        
    - name: Baixar e Processar CMED
      run: |
        node .github/scripts/sync-cmed.js
        
    - name: Verificar se houve mudanças
      id: verify-changed-files
      run: |
        if [ -n "$(git status --porcelain)" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
        else
          echo "changed=false" >> $GITHUB_OUTPUT
        fi
        
    - name: Commit e Push
      if: steps.verify-changed-files.outputs.changed == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/
        git commit -m "🔄 Atualização automática base CMED - $(date '+%Y-%m-%d')"
        git push
        
    - name: Notificar sucesso
      if: steps.verify-changed-files.outputs.changed == 'true'
      run: |
        echo "✅ Base CMED atualizada com sucesso!"
        echo "📊 Nova versão disponível para o backend"
